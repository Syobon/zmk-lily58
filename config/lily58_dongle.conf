# Bluetooth & USB両方有効
CONFIG_ZMK_BLE=y
CONFIG_ZMK_USB=y

CONFIG_ZMK_SLEEP=y

# Prospector LCD用 LVGL設定
CONFIG_LV_COLOR_DEPTH_16=y

# Display有効化(Prospector用)
CONFIG_ZMK_DISPLAY=y

# 環境光センサーを無効化して固定輝度に
CONFIG_PROSPECTOR_USE_AMBIENT_LIGHT_SENSOR=n
CONFIG_PROSPECTOR_FIXED_BRIGHTNESS=100