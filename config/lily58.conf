# OLED無効(OLED未実装のため)
CONFIG_ZMK_DISPLAY=n

# Bluetooth & USB両方有効
CONFIG_ZMK_BLE=y
CONFIG_ZMK_USB=n

# スリープ有効(省電力)
CONFIG_ZMK_SLEEP=y

# 将来のProspector対応に向けて、display-name対応
# (現時点では使われないが、書いても問題ない)

# ZMK Studio 設定(ZMK v0.3 では非対応のためコメントアウト)
# CONFIG_ZMK_STUDIO=y
# CONFIG_ZMK_STUDIO_LOCKING=n